pre-commit:
  parallel: true
  commands:
    linter: # lint using eslint
      run: npx nx affected --target=lint --uncommitted

    formatter: # check formatting using prettier
      run: npx nx format:check --uncommitted

    stylelint:
      run: npx nx affected --target=stylelint --uncommitted
