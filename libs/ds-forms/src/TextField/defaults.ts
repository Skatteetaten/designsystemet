import { FormSize } from '@skatteetaten/ds-core-utils';

import { TextFieldAs } from './TextField.types';

export const getTextFieldAsDefault = (): TextFieldAs => 'input';
export const getTextFieldVariantDefault = (): FormSize => 'medium';
