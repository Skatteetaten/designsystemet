import { Meta, Primary, Controls } from '@storybook/blocks';
import { PlayroomCanvas as Canvas } from '../../playroom/utils';
import * as PaginationStories from './Pagination.stories';
import { VersionBadge } from '../utils/VersionBadge';
import { DocIntro, Link } from '../utils/DocIntro';
import { DocHeaderWrapper } from '../utils/DocHeaderWrapper';

<Meta of={PaginationStories} />

<DocHeaderWrapper componentName="Pagination">

# Pagination

<VersionBadge packageName={'navigation'} />

<DocIntro
  name="Pagination"
  url="https://www.skatteetaten.no/stilogtone/designsystemet/komponenter/pagination/"
/>

</DocHeaderWrapper>

Hvordan bruke <Link url="#hvordan-bruke-props-i-kombinasjon-med-andre-props" target="_self">props</Link> i komponenten

<Primary />

```tsx
import { Pagination } from '@skatteetaten/ds-navigation';
```

<Controls />

## Hvordan bruke props i kombinasjon med andre props

Komponenten vil vise summary og lenker i de tilfellene hvor det er mulig å regne ut. Vi har noen regler som må følges for at utregning skal være mulig. Denne listen med regler viser hva som forhindrer komponenten å rendres:

### totalItems / pageSize < currentPage

Dette betyr at gjeldende side må være større enn totalt antall elementer delt på sidestørrelse.

### currentPage < 1

Aktiv side kan ikke være mindre enn side 1

### totalItems < 1

Komponenten skal ikke vises hvis det ikke eksisterer noen sider man kan navigere til. Vi har i tillegg en regel som bare skjuler pagineringslenkene hvis totalItems er større enn pageSize

### pageSize < 1

Paginering skal bare vises hvis man har spesifisert antall sider til å være én eller mer.

## Controlled Example

<Canvas of={PaginationStories.Controlled} />

## Uncontrolled Example

<Canvas of={PaginationStories.Uncontrolled} />
