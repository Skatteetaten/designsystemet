import { Meta, Primary, Controls } from '@storybook/blocks';
import { PlayroomCanvas as Canvas } from '../../playroom/utils';
import * as BlockquoteStories from './Blockquote.stories';
import { VersionBadge } from '../utils/VersionBadge';
import { DocIntro } from '../utils/DocIntro';

<Meta of={BlockquoteStories} />

# Blockquote

<VersionBadge packageName={'typography'} />

<DocIntro
  name="Blockquote"
  url="https://www.skatteetaten.no/stilogtone/designsystemet/komponenter/typography/#blockquote"
/>

<Primary />

```tsx
import { Blockquote } from '@skatteetaten/ds-typography';
```

<Controls />

## Examples

<Canvas of={BlockquoteStories.Examples} />
