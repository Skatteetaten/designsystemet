import { Meta, Primary, Controls } from '@storybook/addon-docs/blocks';
import { PlayroomCanvas as Canvas } from '../../playroom/utils';
import * as CardStories from './Card.stories';
import { VersionBadge } from '../utils/VersionBadge';
import { DocIntro } from '../utils/DocIntro';
import { DocHeaderWrapper } from '../utils/DocHeaderWrapper';

<Meta of={CardStories} />

<DocHeaderWrapper componentName={'Card'}>

# Card

<VersionBadge packageName={'content'} />

<DocIntro url="https://www.skatteetaten.no/stilogtone/designsystemet/komponenter/card/" />

</DocHeaderWrapper>

<Primary />

```tsx
import { Card } from '@skatteetaten/ds-content';
```

<Controls />

## Skjemakort

<Canvas of={CardStories.Examples} />

## Liste med filer

<Canvas of={CardStories.FileListExample} />

## Hvordan bruke Card.Alert

Pass på å sette riktig heading-nivå i Card.Alert. Vanligvis vil dette være samme nivå som hovedheading i Card. Når Card.Alert lukkes må man huske å sette fokus. Vanligvis kan fokus settes til hovedheading i Card.

<Canvas of={CardStories.AlertExample} />
