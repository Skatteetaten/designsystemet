import { Meta, Canvas, Primary, Controls } from '@storybook/blocks';
import * as CardStories from './Card.stories';
import { VersionBadge } from '../utils/VersionBadge';
import { DocIntro } from '../utils/DocIntro';

<Meta of={CardStories} />

# Card

<VersionBadge packageName={'content'} />

<DocIntro
  name="Card"
  url="https://www.skatteetaten.no/stilogtone/designsystemet/komponenter/card/"
/>

<Primary />

```tsx
import { Card } from '@skatteetaten/ds-content';
```

<Controls />

## Examples

<Canvas of={CardStories.Examples} />

## Hvordan bruke Card.Note

Pass på å sette riktig heading-nivå i Card.Note. Vanligvis vil dette være samme nivå som hovedheading i Card. Når Card.Note lukkes må man huske å sette fokus. Vanligvis kan fokus settes til hovedheading i Card.

<Canvas of={CardStories.NoteExample} />
