import { Meta, Canvas, Primary, Controls } from '@storybook/blocks';
import * as StepListStories from './StepList.stories';
import { VersionBadge } from '../utils/VersionBadge';

<Meta of={StepListStories} />

# StepList

<VersionBadge packageName={'collections'} />

<Primary />

```tsx
import { StepList } from '@skatteetaten/ds-collections';
```

<Controls />

## Examples

<Canvas of={StepListStories.Examples} />
