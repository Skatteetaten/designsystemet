import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as OpenCloseStories from './OpenClose.stories';

<Meta of={OpenCloseStories} />

# OpenClose

<Controls />

## Default

<Canvas of={OpenCloseStories.Default} />

## Example

<Canvas of={OpenCloseStories.Example} />
