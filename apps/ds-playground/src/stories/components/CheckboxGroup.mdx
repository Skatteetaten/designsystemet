import { Meta, Canvas, Primary, Controls } from '@storybook/blocks';
import * as CheckboxGroupStories from './CheckboxGroup.stories';
import { VersionBadge } from '../utils/VersionBadge';

<Meta of={CheckboxGroupStories} />

# CheckboxGroup

<VersionBadge packageName={'forms'} />

<Primary />

```tsx
import { CheckboxGroup } from '@skatteetaten/ds-forms';
```

<Controls />

## Examples

<Canvas of={CheckboxGroupStories.Examples} />
