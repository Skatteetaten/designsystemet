import { Meta, Canvas, Primary, Controls } from '@storybook/blocks';
import * as DescriptionListSTories from './DescriptionList.stories';
import { VersionBadge } from '../utils/VersionBadge';
import { DocIntro } from '../utils/DocIntro';

<Meta of={DescriptionListSTories} />

# DescriptionList

<VersionBadge packageName={'content'} />

<DocIntro
  name="DescriptionList"
  url="https://www.skatteetaten.no/stilogtone/designsystemet/komponenter/descriptionlist/"
/>

Merk at hvis komponenten mottar tomme elementer i lista med children så vil den tegne opp tomrom. For å unngå dette kan man filtrere vekk tomme elementer før de sendes til komponenten.

<Primary />

```tsx
import { DescriptionList } from '@skatteetaten/ds-content';
```

<Controls />

## Examples

<Canvas of={DescriptionListSTories.Examples} />

## Example with fixed number of columns

Bruk className og sett --description-list-columns til ønsket antall kolonner. Hvis du skal ha et fast antall kolonner må du selv påsse på at det er nok plass til å unngå horisontal scroll.

<Canvas of={DescriptionListSTories.ExampleWithFixedColumns} />
