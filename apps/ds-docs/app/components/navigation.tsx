import { JSX } from 'react';

import styles from './navigation.module.scss';

export const Navigation = (): JSX.Element => {
  return <div className={styles.navCard}>{'Sidemeny kommer her'}</div>;
};
