---
title: Avstander
description: Avstander som brukes til marginer, padding og gap.
---

import spacingJson from '@skatteetaten/ds-core-designtokens/designtokens/spacing.json';
import { TokenTable } from '../../../../app/components/token-table';

<TokenTable
  caption={'Avstandstokens'}
  tokens={Object.fromEntries(
    Object.entries(spacingJson[':root,\n:host']).filter(
      ([name]) => !name.includes('semantic')
    )
  )}
/>
